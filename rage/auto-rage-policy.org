#+title: org-vector AUTO RAGE execution policy
#+startup: overview

* Authority
- Program :: org-vector v2 / issue #2
- Mode :: AUTO RAGE
- Human gate :: disabled
- Effective :: 2026-08-29

* Execution rule
ARADR research may proceed directly into design, RED test construction, implementation, verification, documentation, and the next coherent slice without waiting for human approval between stages.

The worker should not stop merely because a research/design document has become ready for implementation. Once acceptance criteria are materially defined, it should write the failing deterministic test, prove RED for the intended missing behavior, implement the smallest coherent change, prove GREEN, record exact-head evidence, and recurse.

* Required discipline
- Research/design remains written to Org files.
- TDD remains RED first; never manufacture RED from an environment failure.
- Do not weaken or delete tests to obtain GREEN.
- Preserve compatibility requirements from issue #2 unless an explicit migration is documented.
- Prefer offline deterministic tests for core behavior.
- Record decisions and exact-head verification evidence in the RAGE/ARADR documents.
- If a slice exposes a dependency, continue on any independent research/design work rather than waiting for a human gate.
- Security-sensitive writes, destructive data migrations, and access to private corpora still require the authority already granted by the surrounding task/repository policy; AUTO RAGE does not invent credentials or permissions.

* Merge authority
No-human-gate applies to ARADR -> design -> RED -> implementation -> verification -> next-slice progression.

Automatic merge is NOT inferred by this file. PR merge remains separately authorized unless the operator explicitly enables merge-on-green for this program.

* Current queue
1. #3 canonical document/chunk/source model + public library API
2. #4 multi-source configuration + generic path discovery
3. generic Org/text adapters + whole-document indexing
4. Tree-sitter code indexing
5. backend abstraction + scale lifecycle
6. Emacs transient/UI + ad-hoc indexing
7. MCP service
8. gptel search + grounded ask
9. benchmark/scale hardening
10. migration/package polish

#4 may deepen research/design while #3 is being realized, but it must consume the canonical source/document types established by #3 rather than inventing a parallel model.
